# Quotas

!!! warning "Verification Required"
    Per-user and per-group quotas for `/home` and `/scratch` have not
    been confirmed. This page is a placeholder.

| Filesystem | Quota |
|---|---|
| `/home` | `TODO: VERIFY WITH HPC ADMINISTRATOR` |
| `/scratch` | `TODO: VERIFY WITH HPC ADMINISTRATOR` |

## Checking your current usage

```bash
du -sh ~
du -sh /scratch/$USER
```

If a formal quota command (e.g. `quota`, `lfs quota`) is enabled on this
cluster, its exact usage is `TODO: VERIFY WITH HPC ADMINISTRATOR`.
