# Network

The cluster's compute nodes are connected via an internal network,
separate from the network administrators use for management and
out-of-band access. Specific IP ranges, switch topology, and export
configuration aren't published here for security reasons.

Shared storage (`/home`) is available on every node via NFS — see
[Storage](../storage/index.md) for user-facing guidance.
