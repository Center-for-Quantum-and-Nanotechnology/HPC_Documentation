# R

!!! warning "Verification Required"
    R availability, version, and module name on this cluster have not
    been confirmed. This page is a placeholder.

## Checking availability

```bash
module avail r
```

If an R module is available, load it and confirm the version:

```bash
module load r
R --version
```

!!! warning "Verification Required"
    TODO: VERIFY WITH HPC ADMINISTRATOR — installed R version, available
    packages/library paths, and whether RStudio Server or another
    interactive front-end is provided.

See [Environment Modules](modules.md) for general module usage.
